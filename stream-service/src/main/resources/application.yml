spring:
  application:
    name: streaming-service
  servlet:
    multipart:
      enabled: true
      max-file-size: 100MB
      max-request-size: 100MB
  datasource:
    url: jdbc:postgresql://localhost:5432/music_files_db
    username: postgres
    password: verystrongpassword
    driver-class-name: org.postgresql.Driver
    initialization-mode: always
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8084

file:
  rootPath: /C:/Users/Munchausen/IdeaProjects/MusicSufferService/music